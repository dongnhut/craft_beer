$("#category-modal .modal-title").html("<%= @user.name %>");
$("#category-modal .modal-body #email").html("<%= @user.email %>");
$("#category-modal .modal-body #address").html("<%= @user.address %>");

